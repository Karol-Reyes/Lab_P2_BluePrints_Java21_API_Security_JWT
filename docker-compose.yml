services:
  db:
    image: postgres:18
    container_name: postgres-dev
    environment:
      POSTGRES_PASSWORD: JDKX240524
      POSTGRES_USER: MorenoRodriguez
      POSTGRES_DB: BluePrints
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
volumes:
  postgres_data: